services:
  backend:
    build: ./Backend
    ports:
      - "8000:3000"
    volumes:
      - ./Backend:/app
      - backend_node_modules:/app/node_modules

    command: npx nodemon -L src/server.js
volumes:
  backend_node_modules:
